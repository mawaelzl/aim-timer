import App from './App';
import * as ReactDOM from "react-dom";
import * as React from "react";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
